import React from "react";

import { AppLayoutContainer, MainNavigation } from "containers";

export default function Layout() {
  return (
    <AppLayoutContainer>
      <MainNavigation />
    </AppLayoutContainer>
  );
}
