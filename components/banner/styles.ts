import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    // ...globalStyles.centered,
    borderRadius: 8,
    padding: 16,
  },
  xIcon: {
    position: "absolute",
    right: 8,
    top: 8,
  },
});
