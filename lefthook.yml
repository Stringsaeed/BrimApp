pre-commit:
  parallel: true
  commands:
    linter:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm lint
    format:
      glob: "*.{js,jsx,ts,tsx,json,css,scss}"
      run: pnpm exec biome check --write {staged_files}
    # tests:
    #   run: pnpm run test --passWithNoTests --findRelatedTests {staged_files}

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
